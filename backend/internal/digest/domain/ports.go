package domain

// This file previously defined DigestApplication and SourceRepository,
// both of which were dead code. ContentSource in source.go is the
// real seam (RSS + DuckDuckGo implementations).
